[build]
  publish = "."
    command = "cp linkedin-manager-standalone.html index.html"

    [[redirects]]
      from = "/*"
        to = "/index.html"
          status = 200

          [build.environment]
            NODE_VERSION = "18"
            
