[build]
  publish = "."
  command = "echo 'Static site - no build needed'"

[[redirects]]
  from = "/*"
  to = "/linkedin-manager-standalone.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
